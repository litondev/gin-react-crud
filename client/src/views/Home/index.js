import DefaultLayout from "../../layouts/default";

const Home = (props) => {
    return (
        <DefaultLayout
            {...props}>
            <h1>Home</h1>
        </DefaultLayout>
    )
}

export default Home;